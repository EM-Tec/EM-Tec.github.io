# set `baseurl` to your root domain
# if you set it to "/" share icons won't work properly on production
baseurl = "https://EM-Tec.github.io"  # Include trailing slash /
# title = "毛哥EM資訊秘技"  # Edit directly from config/_default/languages.toml # alternatively, uncomment this and remove `title` entry from the aforemention file.
copyright = "2022毛哥EM"
# canonifyurls = true
paginate = 10
theme = "hugo-clarity"
disqusShortname = "em-tec"
DefaultContentLanguage = "zh-tw"
# [languages]
# config/_default/languages.toml

# [menus]
# config/_default/menus/menu.xx.toml

[taxonomies]
category = "categories"
tag = "tags"
series = "series"
